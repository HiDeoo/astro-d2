---
"astro-d2": minor
---

Add native `file` code fence attribute to load external `.d2` sources relative to the Markdown file, with optional `allowedIncludeRoots` configuration for additional include directories under the project.
